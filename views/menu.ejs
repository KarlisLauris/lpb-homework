<!DOCTYPE html>
<html>
  <body>
    <div id="menu">
      <h1><%= title %></h1>
      <% if (typeof items !== 'undefined') { %> <% items.forEach(item => { %>
      <li><%= item %></li>
      <% })}; %>
  </div>
  </body>
</html>
